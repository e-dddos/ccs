#include "ultrasonic.h"

/*  Parameter is a bool pointer to a 4-elements-array. Need to change the values of the array
accordingly to free fields. true = field is free, false - there's an obstacle:
    relatively to the robot's looking direction:
    free_directions[0] - forward
    free_directions[1] - to the right
    free_directions[2] - back
    free_directions[3] - to the left   */
void get_free_directions_from_sensors(bool *free_directions)
{
//Nils' Kram
}